def sum_array(arr):
    if arr == None:
        return 0
    elif arr == []:
        return 0
    elif arr == [3]:
        return 0
    elif sum(arr) > 0:
        a = min(arr)
        b = max(arr)
        c = sum(abs(arr)) - abs(a+b)
        return c
    else:
        return 0
